


mapboxgl.accessToken = mapToken;

const map = new mapboxgl.Map({
    container: "map", // container ID
    // style: "mapbox://style/mapbox/streets-v12",
    center: [81.3000, 24.5301], // starting position [lng, lat]
    zoom: 9 // starting zoom
});

